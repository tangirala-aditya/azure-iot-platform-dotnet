apiVersion: v1
kind: Service
metadata:
  name: grafana
  labels:
    name: grafana-service
spec:
  type: ClusterIP
  ports:
    - port: 8000 
      targetPort: 3000
      protocol: TCP
  selector:
    name: grafana