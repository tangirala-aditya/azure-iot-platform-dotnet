parameters:
  helmVersion:
  subscriptionName:
  resourceGroupName:
  aksName:
  kubernetesNamespace:

steps:
  - task: HelmInstaller@1
    displayName: Install Helm
    inputs:
      helmVersionToInstall: $(helmVersion)
