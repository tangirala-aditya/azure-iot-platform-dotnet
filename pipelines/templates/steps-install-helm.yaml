parameters:
  helmVersion:
  subscriptionName:
  resourceGroupName:
  aksName:
  kubernetesNamespace:

steps:
  - task: HelmInstaller@1
    displayName: Install Helm
    inputs:
      helmVersionToInstall: $(helmVersion)
  
  - script:  |-
      helm version
      helm repo update
    displayName: Updating the Helm Repo  
